import 'package:flutter/material.dart';

class PromptDesignView extends StatelessWidget {
  const PromptDesignView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('prompt-design')),
      body: const Center(child: Text('This is PromptDesignView')),
    );
  }
}
