import 'package:flutter/material.dart';

class RagView extends StatelessWidget {
  const RagView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('rag')),
      body: const Center(child: Text('This is RagView')),
    );
  }
}
